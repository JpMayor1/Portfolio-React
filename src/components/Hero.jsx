const Hero = () => {
    return (
        <main
            id="hero"
            className="w-screen h-screen flex justify-center items-center"
        >
            <h1>Hero</h1>
        </main>
    );
};

export default Hero;
